class Solution(object):
    def twoSum(self, nums, target):
        size = len(nums)
        for i in range(size):
            for j in range(size):
                if (i != j):
                    if(nums[i]+nums[j] == target):
                        return [i,j]
       

        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """
#I did this surprisingly fast. This was my first problem and I solved it in 5 minutes and it got accepted immediately. The problem is it runs in n^2 time. The question is how do lower the runtime and I have no idea how.
        
